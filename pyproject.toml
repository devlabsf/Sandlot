[build-system]
build-backend = "poetry.masonry.api"
requires = ["poetry>=0.12"]

[tool]
[tool.poetry]
authors = ["Lazlo S <lazlos@devlabsf.com>"]
description = ""
name = "sandlot"
version = "0.1.0"
[tool.poetry.dependencies]
python = "^3.8"
termcolor = "^1.1"
[tool.poetry.dev-dependencies]
